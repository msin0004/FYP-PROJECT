		#input from app
		while True: # infinite loop 2
			input = client_socket.recv(buffer).decode('UTF-8') # receive client data into buffer

			
			if (input == 'red'):
				print("red input")
				data.value = 2
			if (input == 'blue'):
				print("blue input")
				data.value = 2
			if (input == 'yellow'):
				print("yellow input")
				data.value = 2
			if (input == 'green'):
				print("green input")
				data.value = 2
			if (input == 'orange'):
				print("orange input")
				data.value = 2
			if (input == 'update'):
				print("Sending data")
				#sending = 'sending data from server to client'
				central_authority_broadcast[0] = 'No Advice'
				#temporary[testing]
				client_socket.send(central_authority_broadcast[0] + '\n') # send the data to the client
				#client_socket.send(sending.encode())
				#client_socket.send(sending + '\n')
				print(sending)
				print(sending.encode())
				data.value = 10

